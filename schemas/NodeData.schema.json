{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title" : "ItemNodeSaveData",
  "type" : "object",
  "required": ["itemId", "totalOwned", "isUnlocked"],
  "properties": {
    "itemId" : {
      "type" : "string",
      "description": "Identifier of the item that this node corresponds to / holds."
    },
    "totalOwned" : {
      "type" : "integer",
      "description": "The total number of copies this node holds of the item."
    },
    "isUnlocked" : {
      "type" : "boolean",
      "description": "Whether or not this item is able to be obtained from other systems."
    }
  }
}

